import './TopBar.css'

function TopBar() {
  return (
    <header className="topbar">
      {/* Minimized TopBar as per requirements: No dots, no icons, no tabs */}
    </header>
  )
}

export default TopBar
